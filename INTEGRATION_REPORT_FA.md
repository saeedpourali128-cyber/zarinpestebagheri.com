# گزارش ادغام ریدیزاین زرین دانه ترشیز

## نتیجه

ریدیزاین تصویری در ساختار اصلی پروژه Next.js ادغام شده است. مسیرها، داده‌های محصولات، ساختار چندزبانه `next-intl`، متادیتا، JSON-LD، کاتالوگ و منطق دکمه‌های استعلام قیمت حفظ شده‌اند.

## بخش‌های بازطراحی‌شده

- هدر سبز تیره با لوگوی طلایی و پس‌زمینه هماهنگ
- نوار زبان، منوی دسکتاپ و منوی موبایل
- صفحه اصلی با هیرو، ویژگی‌ها، محصولات منتخب، کارخانه، گواهی‌ها، دانشنامه و CTA
- صفحه جزئیات محصولات با گالری تعاملی، مشخصات، کاربردها، محصولات مرتبط و راهنمای خرید
- صفحه محیط کارخانه با تصاویر کارخانه، تجهیزات، گواهی‌ها و مزیت‌ها
- فوتر جدید هماهنگ با هویت بصری سبز، کرم و طلایی
- تصاویر محصولات، کارخانه، دستگاه‌ها، گواهی‌ها و مقالات
- Open Graph و آیکن با رنگ‌های جدید برند

## فایل‌های تصویری

تصاویر رابط از عکس‌های موجود در اسکرین‌شات‌های مرجع ارسالی استخراج و در مسیر زیر قرار گرفته‌اند:

```text
public/images/redesign/
```

هیچ بخش فعال سایت در داده‌های فعلی بدون تصویر نیست. برای کیفیت نهایی در نمایشگرهای بزرگ، بهتر است نسخه اصلی و باکیفیت عکس‌های محصولات و کارخانه بعداً با همین نام فایل‌ها جایگزین شود.

## آزمون‌های انجام‌شده

موفق:

```bash
npm run lint
./node_modules/.bin/tsc --noEmit
```

همچنین تمام مسیرهای محلی تصاویر در کد بررسی شدند و فایل مفقودی وجود ندارد.

## وضعیت Build در محیط بررسی

فایل `node_modules` داخل بسته اولیه روی ویندوز ساخته شده بود و فقط باینری‌های Native ویندوز را داشت. به همین دلیل اجرای `next build` در محیط لینوکسی بررسی‌کننده به باینری‌های Linux مربوط به SWC و Parcel دسترسی نداشت. این مورد خطای کد پروژه نیست.

در سیستم اصلی، پوشه‌های تولیدشده قدیمی را حذف و وابستگی‌ها را مجدداً نصب کنید:

```bash
rm -rf node_modules .next
npm install
npm run lint
npx tsc --noEmit
npm run build
npm run dev
```

در PowerShell ویندوز:

```powershell
Remove-Item node_modules,.next -Recurse -Force -ErrorAction SilentlyContinue
npm install
npm run lint
npx tsc --noEmit
npm run build
npm run dev
```

## اطلاعاتی که هنوز باید واقعی شوند

- شماره تلفن و واتس‌اپ در `src/lib/config/site.ts` و `src/lib/config/whatsapp.ts`
- نشانی دقیق و ایمیل
- مشخصات عددی هر محصول، مانند انس، حداقل سفارش و نوع بسته‌بندی
- نسخه رسمی گواهی‌ها و مجوزها
- تصاویر اصلی با رزولوشن بالا در صورت موجود بودن

## نکته جایگزینی

این نسخه یک پروژه کامل و ادغام‌شده است. بهتر است فایل‌های ZIP را روی پروژه فعلی بازنویسی نکنید؛ ابتدا پروژه فعلی را Backup بگیرید و این نسخه را در یک پوشه یا Branch جدا اجرا و Build کنید.
