# اجرای پروژه

```bash
npm install
npm run dev
```

سایت به‌صورت پیش‌فرض در آدرس زیر اجرا می‌شود:

```text
http://localhost:3000/fa
```

آزمون قبل از انتشار:

```bash
npm run lint
npx tsc --noEmit
npm run build
```
